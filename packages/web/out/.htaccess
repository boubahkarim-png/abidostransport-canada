RewriteEngine On
RewriteBase /

# Handle trailing slashes
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html [L]

# Default page
DirectoryIndex en.html index.html

# Disable directory listing
Options -Indexes
